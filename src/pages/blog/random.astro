---
export const prerender = false;

import { getCollection } from "astro:content";

const blog = await getCollection("blog");
const slug = blog[Math.floor((Math.random() * blog.length))].slug

return Astro.redirect(`/blog/${slug}`);
---
